# سجل مصدر البيانات وتحققها

ملف التدريب المضمن هو `qwen25_3b_semantic_colab.jsonl.gz`. أُنشئ من `book.md` القانوني اليمني الخام مع الحفاظ على المصدر وعدم إضافة وقائع قانونية خارجية. بصمة المصدر الخام `book.md` هي:

```text
3d95499916f84ab93001428adfa1c0bce8ea44d618b7f627b6fe154b6e1a9b62
```

## نتائج الفحص الموجود داخل الحزمة

| الفحص | النتيجة |
|---|---:|
| عدد السجلات | 165,303 |
| الحقل المحادثي | `messages` موجود في كل سجل صالح |
| الأدوار | `system`, `user`, `assistant` |
| وجود إجابة مساعد | موجود في كل سجل |
| أكبر `token_count_qwen25_chat` | 850 |
| حد المدرب الافتراضي | 1024 |
| ضغط الملف | gzip، JSONL |
| SHA-256 للملف المضغوط المضمن | `0786cdd255800427cc143c4001f7b67a12838c2224d8de1799115a28f29cd860` |

إعادة التحقق من داخل مجلد الحزمة:

```bash
python verify_bundle.py
```

يوقف الفحص التنفيذ إذا تغيّر عدد السجلات، أو تجاوزت قيمة `token_count_qwen25_chat` الحد 1024، أو غابت رسالة المساعد، أو ظهرت أدوار غير معتمدة. أما التحقق النهائي من tokenization أثناء التدريب فينفذه `train.py` باستخدام chat template الفعلي للنموذج، ويرفض أي سجل يتجاوز `max_seq_length` بدلاً من قصه.
