# تقرير مراجعة مستودع Qwen2.5-3B-Yemeni-MoRA

**النطاق:** مراجعة ملفات التدريب، إعدادات MoRA/QLoRA، أداة التحقق، دفتر Google Colab، وحزمة `peft-mora` المحلية في الفرع `moranew`.

**النتيجة التنفيذية:** لم تُكتشف أخطاء صياغة Python أو أخطاء حرجة في نقطة الدخول بعد تشغيل التدقيق الثابت واختبار الترجمة البرمجية. كما نجح اختبار الدخان السابق على 128 سجلًا وخمس خطوات، ونجح تدريب مستهدف قصير على 256 سجلًا قانونيًا. لكن توجد سبع ملاحظات تشغيلية ووثائقية مهمة، أبرزها عدم تجانس مخطط JSONL ومسارات البيانات القديمة في دفتر Colab. هذه المشكلات لا تعني أن خوارزمية MoRA نفسها خاطئة؛ بل تمنع أو تربك التشغيل المتكرر ما لم تُوحّد البيانات ومسارات الاستيراد.

> **حدود المراجعة:** هذا التقرير يراجع سلامة خط التدريب وتشغيله، ولا يثبت أن النموذج أصبح محاميًا متمكنًا أو أنه يقدم رأيًا قانونيًا صحيحًا في كل واقعة. جودة الإجابة القانونية تحتاج مجموعة تقييم مستقلة، ومراجعة بشرية من مختص بالقانون اليمني، واختبارات مصدرية تمنع اختلاق المواد.

## 1. الملفات والأدوات التي تمت مراجعتها

| المكوّن | النتيجة |
|---|---|
| `train_qwen_mora.py` | سليم صياغيًا، ويطبّق قالب Qwen وقناع loss لرسائل `assistant` ويرفض القص الصامت. |
| `train.py` | مطابق تمامًا لـ `train_qwen_mora.py`، وليس مدربًا مستقلًا. |
| `configs/qwen25_3b_mora.json` | إعدادات QLoRA/MoRA مناسبة مبدئيًا لـ T4، لكن مسار البيانات افتراضي محلي قديم. |
| `colab_train_qwen25_3b_mora.ipynb` | بنية الخلايا صحيحة، لكن بعض الخلايا تشير إلى الملف غير الموحد، وخلية الاختبار تحتاج تثبيت مسار fork داخل النواة. |
| `peft-mora/setup.py` و`pyproject.toml` | اكتشاف حزم `src/peft` صحيح؛ المشكلة المرصودة في Colab مرتبطة بإعادة تحميل مسار Python بعد التثبيت داخل النواة نفسها. |
| `verify_bundle.py` | يتحقق من حزمة مضغوطة `JSONL.GZ`، وليس من نسخة JSONL غير المضغوطة التي استُخدمت فعليًا في Colab. |
| `README_QWEN.md` | يشرح المسار العام جيدًا، لكنه يذكر أسماء ومسارات لا تطابق تمامًا مسار Colab الموحد. |

تم تشغيل `audit_repo.py` وكانت النتيجة `TOTAL_ISSUES=0` للمشكلات الحرجة، كما نجح `compileall` لجميع ملفات Python الرئيسية. تعني هذه النتيجة عدم وجود فشل ساكن واضح، ولا تعني غياب مشاكل البيانات أو البيئة أو جودة التدريب.

## 2. مصفوفة المشكلات

| الرقم | الأولوية | الموضع | المشكلة | الأثر |
|---:|---|---|---|---|
| 1 | **HIGH** | `train_qwen_mora.py:357` | `datasets.load_dataset("json")` لا يتعامل بأمان مع الملف الأصلي ذي الأعمدة غير المتجانسة. | قد يتوقف تحميل البيانات بـ`DatasetGenerationCastError` قبل بدء التدريب. |
| 2 | **HIGH** | خلية التثبيت وخلية الاختبار في الدفتر | التثبيت editable لا يضيف مسار `src` إلى `sys.path` الجاري في نواة Colab الحالية. | قد تستورد الخلية نسخة PEFT القياسية أو تفشل بـ`ModuleNotFoundError`، بينما يعمل subprocess الخاص بالتدريب. |
| 3 | **MEDIUM** | خلايا التنزيل والتحقق والتدريب والاختبار | بعض الخلايا تستخدم `qwen25_3b_semantic_colab.jsonl`، بينما الملف الذي اجتاز التوحيد هو `qwen25_3b_semantic_colab_train.jsonl`. | احتمال إعادة إنتاج مشكلة المخطط الأصلي أو تدريب ملف مختلف عن الملف الذي تم التحقق منه. |
| 4 | **MEDIUM** | `train.py` و`train_qwen_mora.py` | الملفان متطابقان حرفيًا؛ SHA-256 لكليهما هو `d34ddc556cdfa029d35106c21c963ce366a120db672ab94d372f384a69625aea`. | ازدواج صيانة، واحتمال تعديل أحد الملفين دون الآخر، وارتباك في README. |
| 5 | **LOW** | `verify_bundle.py` | الأداة تفترض `gzip.open()` وتتحقق من SHA-256 للملف المضغوط فقط. | لا يمكن استخدامها مباشرة للتحقق من ملف JSONL غير المضغوط في جلسة Colab. |
| 6 | **LOW** | `configs/qwen25_3b_mora.json:3` | `data_path` يشير إلى `./qwen25_3b_semantic_colab.jsonl.gz`، بينما Colab ينزّل JSONL غير مضغوطًا ثم ينشئ نسخة موحدة باسم آخر. | التشغيل المحلي من الإعدادات الافتراضية قد يفشل بسبب مسار غير موجود أو ملف غير مناسب. |
| 7 | **INFO** | خلية اختبار adapter، شرط `task_type` | تبحث الخلية عن `record.get("task_type") == "rule"`، مع أن التصنيف في النسخة الموحدة داخل `metadata_json` وليس بالضرورة في المستوى الأعلى. | قد لا تجد سجل الاختبار رغم وجود سجلات مناسبة، فتفشل بـ`assert test_record is not None`. |

## 3. شرح الإصلاحات

### 3.1 عدم تجانس JSONL — أولوية HIGH

في `train_qwen_mora.py:357` يُمرّر الملف مباشرة إلى `load_dataset("json")`. عندما تحمل بعض الأسطر مفاتيح إضافية مثل `page_number` و`document_id` وتحذفها أسطر أخرى، قد يحاول مُحمّل Datasets بناء مخطط موحّد ثم يفشل في تحويل الأعمدة. هذه ليست مشكلة في محتوى `messages`، بل في اختلاف مخطط السجلات.

**الإصلاح المعتمد:** إنشاء ملف تدريب موحد يحتوي على `messages` كاملة و`metadata_json` كسلسلة JSON واحدة. بهذه الطريقة يرى المحمّل عمودين ثابتين فقط، ويبقى المحتوى القانوني كاملًا دون حذف الرسائل. يجب أن يظل الملف الموحد هو المدخل الرسمي للتدريب في Colab، مع التحقق من عدد السجلات `165,303` وبصمة SHA-256 قبل التشغيل.

**الإصلاح الأفضل على المدى الطويل:** إضافة خطوة تطبيع صريحة داخل الدفتر قبل `load_dataset`، وعدم الاعتماد على أن ملف المصدر الخام متجانس. أما تعديل المدرب نفسه لإجبار Datasets على مخطط ديناميكي فليس الخيار المفضل، لأنه قد يخفي اختلافًا في بنية البيانات بدلاً من كشفه.

### 3.2 مسار `peft-mora` داخل نواة Colab — أولوية HIGH

حزمة `peft-mora` تستخدم بنية `src/peft`، وقد أكد الفحص المحلي أن `setuptools.find_packages("src")` يكتشف حزم PEFT المطلوبة. لذلك لا توجد قرينة على أن `setup.py` أسقط الحزمة. المشكلة التشغيلية هي أن `pip install -e ./peft-mora` يحدّث ملفات البيئة، لكن عملية Python الجارية في دفتر Jupyter لا تعيد قراءة ملفات `.pth` تلقائيًا.

**الإصلاح المعتمد في خلية الاختبار:** إضافة:

```python
import sys
sys.path.insert(0, "/content/qwen25-3b-yemeni-mora/peft-mora/src")
```

قبل `from peft import PeftModel`. ويجب فحص:

```python
import peft
from peft import LoraConfig
assert "use_mora" in getattr(LoraConfig, "__dataclass_fields__", {})
print(peft.__file__)
```

هذا يمنع اختبار adapter باستخدام PEFT القياسي دون MoRA. أما مسار التدريب الذي يُشغّل `!python train.py` في subprocess مستقل، فيقرأ البيئة المحدثة عادة، لكن يبقى فحص `use_mora` داخل `train_qwen_mora.py` ضروريًا كحاجز أمان.

### 3.3 توحيد مسارات الدفتر — أولوية MEDIUM

ينبغي تعريف متغير واحد مثل:

```python
DATA_PATH = DATA_DIR / "qwen25_3b_semantic_colab_train.jsonl"
```

ثم استخدامه في خلية التحقق والتدريب والاختبار والاستئناف. لا ينبغي أن تظل أسماء الملف الأصلي والملف الموحد متداخلة، لأن نجاح التحقق من ملف ثم تدريب ملف آخر يضعف قابلية إعادة الإنتاج.

### 3.4 إزالة ازدواج نقطة الدخول — أولوية MEDIUM

بما أن `train.py` و`train_qwen_mora.py` متطابقان، فالحل الأنظف هو الاحتفاظ بـ`train_qwen_mora.py` كنقطة الدخول الموصوفة، وجعل `train.py` غلافًا قصيرًا يستدعي `main`، أو حذف أحدهما بعد تحديث README والدفتر. إلى أن يتم ذلك، يجب اعتبار التطابق اختبارًا مستمرًا في CI حتى لا ينفصل الملفان مستقبلًا.

مثال غلاف صريح:

```python
from train_qwen_mora import main

if __name__ == "__main__":
    main()
```

### 3.5 توسيع أداة التحقق — أولوية LOW

ينبغي أن تدعم `verify_bundle.py` الامتدادين `.jsonl` و`.jsonl.gz`، وأن تميّز بين `content_sha256` للبيانات المفكوكة و`archive_sha256` للملف المضغوط. هذا يمنع مقارنة بصمة الحاوية المضغوطة ببصمة المحتوى المفكوك. كما ينبغي أن تتحقق من أن `messages` موجودة، وأن لكل سجل رسالة `assistant` غير فارغة، وأن عدد السجلات يطابق القيمة المتوقعة.

### 3.6 مسار الإعدادات — أولوية LOW

الإعداد الافتراضي الحالي مناسب لحزمة محلية تحتوي على `qwen25_3b_semantic_colab.jsonl.gz`، لكنه ليس اسم ملف Colab الموحد. الحل الأقل خطورة هو إبقاء الإعداد المحلي كما هو، مع تمرير `--data_path` في الدفتر إلى الملف الموحد، وتعديل README بوضوح. أما تغيير قيمة الإعداد نفسه، فيلزم فقط إذا أُعيدت تسمية ملف الحزمة أو أُضيف الملف الموحد إلى الحزمة.

### 3.7 اختيار سجل الاختبار — أولوية INFO

بدل البحث عن `task_type` في المستوى الأعلى، يجب قراءة `metadata_json` إن وجد، ثم استخدام أول سجل مناسب، أو استخدام سؤال ثابت معروف المصدر لتجنب اختلاف نتائج الاختبار بين التشغيلات. مثال آمن:

```python
metadata = json.loads(record.get("metadata_json", "{}"))
task_type = record.get("task_type", metadata.get("task_type"))
if task_type == "rule":
    test_record = record
```

ومع ذلك، لا ينبغي اعتبار شرط `rule` ضمانًا لجودة الاختبار؛ الأفضل حفظ سؤال قانوني ثابت، ثم تقييم الإجابة بمصفوفة معايير تشمل ذكر القاعدة، التمييز بين الإقرار والشهادة، وعدم اختلاق مادة أو حكم.

## 4. ملاحظات جودة التدريب وليست أعطالًا تشغيلية

أولًا، `validation_size` الافتراضي يساوي صفرًا، ولذلك لا توجد مجموعة تحقق في التدريب الكامل. هذا يسمح بالتدريب، لكنه لا يسمح بقياس التعميم أو اكتشاف الحفظ. يوصى بتخصيص تقسيم ثابت، مثل 1% أو 2%، بعد ضمان عدم تسرب نسخ متشابهة من النص نفسه بين التدريب والتحقق.

ثانيًا، قناع loss المطبق في `train_qwen_mora.py:321-339` يحافظ على رسائل `system` و`user` كسياق ويحسب الخسارة على `assistant` فقط. هذه نقطة صحيحة لمسار المحادثة، لكن جودتها تعتمد على صحة قالب Qwen؛ ولهذا يتوقف الكود إذا لم يستطع تحديد نطاق المساعد. لا ينبغي إزالة هذا الفحص أو استبداله بقص يدوي لمعرفات special tokens.

ثالثًا، رفض السجل الأطول من `max_seq_length` في `train_qwen_mora.py:314-319` أفضل من القص الصامت من منظور قانوني. إذا ظهر سجل أطول بعد إعادة توليد البيانات، يجب تقسيمه دلاليًا قبل التدريب، لا خفض الطول آليًا. سجل التدريب الحالي اجتاز تدقيقًا سابقًا بحد أقصى 1024 token وفق الحقل المخزن، لكن المدرب يعيد تطبيق قالب Qwen ويحسب الطول من جديد، وهو الاختبار الحاسم.

رابعًا، التدريب الكامل بالإعدادات الافتراضية على T4 المجاني ليس تجربة قصيرة. زمن التشغيل المقدّر سابقًا يقارب 119 ساعة، لذلك ينبغي استخدام checkpoints والاستئناف، أو تشغيل مرحلة أولى أصغر لتقييم الفائدة قبل استهلاك جلسات Colab المتعددة. الـadapter السريع بــ256 سجلًا و10 خطوات هو نموذج إثبات تشغيل، وليس نموذجًا قانونيًا نهائيًا.

## 5. قائمة قبول قبل التدريب الكامل

| الاختبار | معيار القبول |
|---|---|
| البصمة والعدد | `165303` سجلًا، وبصمة الملف الموحد مطابقة للقيمة المسجلة. |
| المخطط | كل سجل يملك `messages` غير فارغة، وبها `assistant` غير فارغة، و`metadata_json` صالح إن وُجد. |
| الاستيراد | `peft.__file__` يشير إلى `peft-mora/src/peft`، و`LoraConfig` يملك `use_mora`. |
| اختبار الدخان | خمس خطوات على الأقل دون OOM أو خطأ في chat template أو loss mask. |
| التحقق من الحدود | لا يوجد سجل يعبر `max_seq_length=1024` بعد إعادة التوكنة. |
| الحفظ | وجود `adapter_config.json` وملفات أوزان adapter وtokenizer و`training_metadata.json`. |
| الاستئناف | إنشاء checkpoint صالح ثم استئناف خطوة لاحقة دون فقدان حالة optimizer أو scheduler. |
| الاختبار القانوني | توليد إجابة من adapter مع تسجيل السؤال والإجابة وإصدار النموذج والـadapter. |

## 6. الخلاصة

المستودع قابل للتشغيل من الناحية البرمجية، وقد أثبتت اختبارات الدخان والتدريب السريع أن مسار Qwen2.5-3B + QLoRA + MoRA يعمل على T4 عند استخدام الملف الموحد واستيراد fork المحلي. العائقان الحقيقيان قبل التدريب الكامل هما **تثبيت مخطط البيانات** و**توحيد مسار الملف داخل الدفتر**؛ أما مشكلة الاستيراد فهي مشكلة بيئة نواة Colab وليست دليلًا على فساد `peft-mora`.

لا يوصى ببدء التدريب الكامل قبل تطبيق تحديثات الدفتر، لأن تدريب عشرات الساعات على ملف غير موحد أو مسار غير صحيح قد يهدر جلسة Colab. بعد التحديث، يُستخدم الـadapter السريع للاختبار الوظيفي فقط، ثم يبدأ التدريب المرحلي مع checkpoints ومجموعة تحقق مستقلة.

## المراجع

[1]: https://github.com/EngKHALIDx/qwen25-3b-yemeni-mora/tree/moranew "مستودع المشروع، الفرع moranew"

[2]: https://github.com/EngKHALIDx/qwen25-3b-yemeni-mora/blob/moranew/train_qwen_mora.py "مدرب Qwen2.5 وMoRA"

[3]: https://github.com/EngKHALIDx/qwen25-3b-yemeni-mora/blob/moranew/colab_train_qwen25_3b_mora.ipynb "دفتر Google Colab"

[4]: https://github.com/kongds/MoRA "مستودع MoRA الأصلي"

[5]: https://huggingface.co/Qwen/Qwen2.5-3B-Instruct "بطاقة Qwen2.5-3B-Instruct"

[6]: https://huggingface.co/docs/transformers/main/chat_templating "توثيق قوالب المحادثة في Transformers"

[7]: https://huggingface.co/docs/peft/developer_guides/quantization "إرشادات PEFT وQLoRA والكمّية"
